Функция ПечатьДоговораЗайма(Ссылка) Экспорт
	
	Пока В  Цикл
	ыборка = Запрос.Выполнить().Выбрать();
	
	Возврат ТабДокумент;
	КонецЦикла;
	
КонецФункции // ПечатьДоговораЗайма()
Функция ПечатьИсторияПогашенияЗайма(Ссылка) 

//jfdoihjgfoij 

	Выборка = Запрос.Выполнить().Выбрать();
	
	Возврат ТабДокумент;
	
КонецФункции // ПечатьИсторияПогашенияЗайма()


Функция ПечатьИсторияПогашенияЗайма(Ссылка) Экспорт
	
	Возврат ТабДокумент;
	
КонецФункции // ПечатьИсторияПогашенияЗайма()

Функция ПечатьИсторияПогашенияЗайма(Ссылка) Экспорт

	Выборка = Запрос.Выполнить().Выбрать();
	
	Возврат ТабДокумент;
	
КонецФункции // ПечатьИсторияПогашенияЗайма()


Функция ПечатьИсторияПогашенияЗайма(Ссылка) Экспорт

//	Выборка = Запрос.Выполнить().Выбрать();
	
	Возврат ТабДокумент;
	
КонецФункции // ПечатьИсторияПогашенияЗайма()
